import { Controller } from '@nestjs/common';
import { ApiTags } from '@nestjs/swagger';

// TODO (Phase 2): Geofence zone manager CRUD endpoints
@ApiTags('Geofences (Phase 2)')
@Controller('geofences')
export class GeofencesController {}
